# Supabase (required for registration form to save)
# Get these from your Supabase project: Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API base URL (for send-email after registration, no trailing slash)
VITE_API_URL=https://your-api-host.com

# Expected table in Supabase: "registrations"
# Columns: full_name (text), email (text), phone (text), college (text), registration_type (text: "attend" | "ambassador")
# Optional: id (uuid, default gen_random_uuid()), created_at (timestamptz, default now())
